# Fleet — 设计稿 V1

> 独立窗口版本，非菜单栏下拉框。本文档记录该版本的完整视觉规范，供实现时参考（原生 SwiftUI 或其他技术栈均可对照这套规范还原）。

## 1. 整体结构

窗口采用**左侧边栏 + 右侧主内容区**的经典两栏布局：

- 侧边栏：固定宽度约 216px，深色背景
- 主内容区：自适应宽度，白色/浅色背景

## 2. 品牌与 Logo

- **Logo**：原创帆船图形——双色三角帆（薄荷绿 + 深绿）+ 桅杆 + 波浪船体线条，搭配衬线字体的 "Fleet" 字标
- **Slogan**（侧边栏副标题）：「你的终端船队」
- 引擎（Claude Code / Codex）图标**不使用官方 logo 的仿制品**，占位阶段用通用图标（对话气泡 / 终端符号）区分；正式开发时应从 Anthropic 和 OpenAI 官方品牌资源页下载正版素材替换

## 3. 配色方案

| 用途 | 颜色 | 色值 |
|---|---|---|
| 侧边栏背景 | 深藏青绿 | `#04342C` |
| 侧边栏强调/选中态 | 中绿 | `#0F6E56` |
| 活跃状态点 / 强调文字 | 薄荷绿 | `#5DCAA5` |
| 活跃 session 卡片背景（Claude 引擎） | 浅薄荷绿 | `#E1F5EE` |
| 活跃 session 卡片背景（Codex 引擎） | 浅紫 | `#EEEDFE` |
| 活跃 session 卡片文字（Codex） | 深紫 | `#3C3489` / `#26215C` |
| 空闲状态 | 中性灰 | `#888780` |
| 疑似过期状态 | 琥珀色 | `#EF9F27` / `#854F0B` |
| 主内容区背景 | 系统浅色表面 | 跟随系统浅色/深色模式 |

**配色逻辑**：深藏青绿 + 薄荷绿构成主品牌色（呼应"船队/航海"主题），紫色仅用于区分 Codex 引擎的强调卡片，琥珀色专门用于"疑似过期"这类需要提醒的状态，不做其他用途扩展。

## 4. 排版

- **品牌标题 / 页面大标题**：衬线字体（如 SwiftUI 中可用 `New York` 或系统衬线字体），营造编辑感，与正文的无衬线字形成对比
  - 侧边栏 "Fleet" 字标：约 19px
  - 主内容区大标题（如"正在运行的船队"）：约 27px
- **正文/组件文字**：无衬线系统字体
  - session 名称（主标题）：14–15px，中等字重
  - 分组小标题（"正在运行" / "空闲" / "疑似过期"）：10.5px，加字间距，弱化颜色
- **技术性元数据**（session ID、文件路径）：等宽字体，11–11.5px，弱化颜色，与 session 名称形成"人类可读 vs 机器信息"的层级区分

## 5. 核心组件

### 5.1 侧边栏
- 顶部：Logo + 品牌名 + slogan
- 状态筛选列表：全部 / 活跃 / 空闲 / 疑似过期，每项右侧显示数量，选中项高亮背景
- 引擎筛选列表：Claude Code / Codex，同样右侧显示数量
- 底部：设置入口
- 装饰细节：底部有一道波浪形色块作为航海主题的呼应

### 5.2 主内容区头部
- 大标题 + 右侧大号数字（如"2 / 6 艘"），突出"当前活跃数"
- 副标题：同步状态说明（如"上次同步 3 秒前 · 全部数据存储在本机"）

### 5.3 活跃 session 聚光灯卡片
- 仅"活跃"状态的 session 用这种卡片样式展示，从列表中独立出来放在头部下方
- 卡片背景使用浅色调（按引擎区分薄荷绿/浅紫），圆角 12px
- 卡片内容：状态点 + 引擎图标 + 活跃时间 → session 备注名 → 元数据（session ID、路径）→ 操作按钮（复制命令 / 恢复，恢复为实心按钮，复制命令为描边按钮）

### 5.4 空闲/过期 session 列表
- 紧凑表格式单行展示，非卡片，视觉重量比活跃卡片低
- 每行：引擎图标 + session 备注名 + 元数据 + 操作按钮（复制/恢复，或对疑似过期项显示"重命名/删除"）
- 按分组（空闲 / 疑似过期）用小标题分隔，行间用细分割线

### 5.5 搜索
- 主内容区顶部搜索框，占位文字："搜索 session、项目或路径..."

## 6. 交互备注（非视觉，供开发参考）

- 「复制命令」：一键复制恢复命令到剪贴板
- 「恢复」：一键打开新终端窗口并自动执行恢复命令
- 「重命名」：编辑 session 的备注名
- 「删除」：从 Fleet 的本地记录中移除该 session 的映射（不影响 Claude Code / Codex 自身的 session 数据）
