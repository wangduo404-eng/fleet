# Fleet — 设计稿 V2（表现主义 / 彩色克制风格）

> 风格参考：Arc 浏览器。核心手法是用饱和但克制的色块 + 大圆角营造活泼感，同时保持排版和间距的秩序感，避免显得杂乱。与「设计稿V1」（深色航海编辑风）是两个独立方向，供对比选择。

## 1. 整体结构

同样是**左侧边栏 + 右侧主内容区**的两栏布局，与 V1 结构一致，区别主要在配色和圆角处理上：

- 侧边栏：浅色背景（非深色），宽度约 224px
- 主内容区：白色/浅色背景，活跃 session 用彩色卡片单独呈现

## 2. 品牌标识

- Logo：紫色圆角方块（`#7F77DD`）+ 白色描边锚图标，替代 V1 的帆船图形，更符合本风格"色块图标"的语言
- 应用名 "Fleet"：无衬线常规字体，不使用衬线体（与 V1 的编辑感衬线标题区分开）

## 3. 配色方案

**核心逻辑**：每个"引擎"或"类别"分配一个专属纯色方块（不是渐变），色块颜色延伸到该类别对应的卡片背景（同色系浅色调），形成"图标色 = 卡片底色"的强关联。

| 用途 | 颜色 | 色值 |
|---|---|---|
| 主强调色（选中态、主要按钮） | 紫 | `#7F77DD`（深色文字用 `#3C3489` / `#534AB7`） |
| Claude Code 引擎标识 | 珊瑚色 | `#D85A30`（浅底 `#FAECE7`，深字 `#993C1D` / `#4A1B0C`） |
| Codex 引擎标识 | 粉色 | `#D4537E`（浅底 `#FBEAF0`，深字 `#993556` / `#4B1528`） |
| 活跃状态点 | 绿 | `#1D9E75` |
| 空闲状态点 | 沿用引擎色（不单独区分） | — |
| 疑似过期状态点 | 琥珀色 | `#EF9F27` |

**配色原则**：全局只保留**一个强调色（紫色）用于主操作按钮**，其余颜色全部承担"分类标识"的功能，不做多重语义叠加，避免颜色滥用导致杂乱。

## 4. 圆角与形状

- 统一圆角区间：10–16px（侧边栏项 11px，卡片 16px，按钮 9–10px），不出现直角或过大圆角
- 引擎/项目标识统一用 **18–26px 圆角方块**（不是圆形头像，也不是线框图标），这是本风格最具识别度的视觉元素

## 5. 核心组件

### 5.1 侧边栏
- Logo + 应用名
- 状态筛选：全部/活跃/空闲/疑似过期，选中项用浅紫底 + 圆点色标；未选中项无背景，仅文字
- 引擎筛选：每项前面是对应引擎的纯色圆角方块图标
- 底部设置入口

### 5.2 主内容区头部
- 标题「你的船队」+ 副标题（航行数量 + 同步状态）
- 右侧唯一的紫色实心按钮：「刷新」

### 5.3 活跃 session 卡片
- 两栏并排网格布局，每张卡片背景色 = 对应引擎的浅色调
- 卡片内：引擎色块 + 状态文字 → session 名称 → 元数据（等宽字体）→ 操作按钮
  - 「复制命令」：白底描边按钮
  - 「恢复」：引擎主色实心按钮

### 5.4 其他 session 列表
- 无卡片包裹的紧凑列表，仅 hover 时给背景反馈
- 每行：引擎色块图标 + session 名 + 元数据 + 单个操作按钮（恢复 / 重命名）

## 6. 与设计稿V1 的对比取舍

| 维度 | V1（航海编辑风） | V2（表现主义/彩色克制） |
|---|---|---|
| 侧边栏基调 | 深色（藏青绿） | 浅色 |
| 标题字体 | 衬线体，编辑感强 | 无衬线，产品感强 |
| 引擎区分方式 | 通用图标占位 | 专属纯色圆角方块 |
| 情绪基调 | 沉稳、有品牌故事感 | 轻快、活泼、克制的彩色 |
| 适合场景 | 想要独特品牌调性、耐看 | 想要日常打开时更有愉悦感 |
