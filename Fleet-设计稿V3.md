# Fleet — 设计稿 V3（深海蓝毛玻璃风）

> 风格参考：Arc 浏览器的毛玻璃质感（渐变桌面背景 + 悬浮半透明玻璃面板），配色改为深海蓝，布局采用左侧边栏 + 右主内容区（区别于 Arc 原版的多面板并排结构）。

## 1. 整体结构

- 整个界面模拟一个 **macOS 窗口**：顶部有红黄绿交通灯按钮 + 半透明标题栏
- 窗口背景是**深蓝色桌面**，而非纯色平面——通过多层半透明色块叠加出层次感（不是真实 CSS 渐变，是分层色块模拟）
- 界面内容分成两块**悬浮玻璃面板**：左侧边栏（固定宽度约 200px）+ 右侧主内容区（自适应宽度），两块面板之间有间距，都悬浮在深蓝背景之上，而不是贴边占满整个窗口

## 2. 背景处理

| 图层 | 说明 | 色值/透明度 |
|---|---|---|
| 底色 | 深蓝色实底 | `#0E2A52` |
| 叠加层 1 | 中蓝色半透明块，覆盖全屏 | `#1B4B8C`，透明度 0.5 |
| 叠加层 2 | 亮蓝色光晕，右上角，圆角遮罩制造光源感 | `#2E6FBF`，透明度 0.28 |
| 叠加层 3 | 深色晕影，左下角，增加明暗对比 | `#0A1A38`，透明度 0.4 |

多层叠加的目的是模拟"渐变桌面壁纸"的丰富度，同时避免使用单一生硬的线性渐变。

## 3. 毛玻璃面板处理（核心质感来源）

每个悬浮面板（侧边栏、主内容区、活跃 session 卡片）遵循同一套"玻璃材质"公式：

- **背景**：白色半透明，`rgba(255,255,255,0.05–0.09)`（内容越重要，透明度可以略高，如活跃卡片用 0.08）
- **边框**：四周用低透明度白色细边框 `rgba(255,255,255,0.1–0.15)`
- **顶部高光**：单独给 `border-top` 设置更高透明度的白色（约 0.25–0.3），模拟光线从上方照在玻璃边缘产生的反光——**这是让"毛玻璃感"成立的关键细节**，缺了这一条只会看起来像普通半透明色块
- **圆角**：面板 12–14px，按钮 7–8px
- 面板内的分割线也用低透明度白色（约 0.08–0.12），不用实色

> 注：这套效果不依赖真实的 `backdrop-filter` 模糊（受限于预览工具的渲染规则），实际用 SwiftUI 实现时应使用系统的 `.ultraThinMaterial` 材质，会得到比这版预览更好的真实模糊效果。

## 4. 配色语言

| 用途 | 颜色 |
|---|---|
| 窗口背景基调 | 深海蓝系（见上表） |
| 活跃状态点 | 薄荷绿 `#5DCAA5` |
| 疑似过期状态点 | 琥珀色 `#FAC775` |
| 空闲状态点 | 半透明白 |
| 主操作按钮（恢复） | 高对比实白 `rgba(255,255,255,0.92)`，文字用背景深蓝色，全局唯一的强对比色块 |
| 次要按钮（复制命令/重命名） | 半透明白描边按钮 |

## 5. 排版

- 全局使用无衬线字体（不同于 V1 的衬线标题方案）
- 正文文字用不同透明度的白色区分层级：主标题 `0.92–0.95`，正文 `0.75–0.85`，元数据/次要信息 `0.4–0.55`
- session ID、路径等技术信息用等宽字体，进一步降低透明度

## 6. 核心组件

### 6.1 窗口顶栏
- 交通灯按钮（红/黄/绿，对应关闭/最小化/最大化）
- 应用名 "Fleet"，低透明度白色文字

### 6.2 侧边栏
- Logo（描边方块 + 锚图标）+ 应用名
- 状态筛选：全部/活跃/空闲/疑似过期，选中项背景加深，圆点变色区分状态
- 分割线
- 引擎筛选：Claude Code / Codex，通用图标占位（同 V1 的原则：不使用官方 logo）

### 6.3 主内容区
- 顶部：标题「正在运行的船队」+ 同步状态
- 活跃 session：两栏卡片网格，浅色玻璃卡片，内含状态点/名称/元数据/操作按钮
- 空闲/过期 session：紧凑列表，细分割线分隔

## 7. 与前两版的关系

这是三版设计稿里最"视觉冲击力强"的一版，代价是实现成本更高（需要精确控制多层透明度和高光细节才能不显得廉价）。如果最终选择这个方向，建议让 Codex 先做一个静态原型验证 `.ultraThinMaterial` 在实际 macOS 窗口里的真实效果，再决定是否全量采用。
